image: node:20

definitions:
  caches:
    node: node_modules

pipelines:
  default:
    - step:
        name: Install
        caches:
          - node
        script:
          - npm ci

  branches:
    testing:
      - step:
          name: Install & Test
          caches:
            - node
          script:
            - npm ci
            - npm test -- --watchAll=false --passWithNoTests

    main:
      - step:
          name: Install & Test
          caches:
            - node
          script:
            - npm ci
            - npm test -- --watchAll=false --passWithNoTests
      - step:
          name: Build
          caches:
            - node
          script:
            - npm ci
            - npm run build
          artifacts:
            - .next/**